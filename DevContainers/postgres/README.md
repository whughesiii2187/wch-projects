# Postgres

Standalone Postgres for shared use across projects.

```bash
make up       # Start Postgres
make down     # Stop Postgres
make cli      # Connect via psql
make logs     # Tail logs
```

Database: `devdb`
User: `dev`
Password: `dev`
Port: `5432`
